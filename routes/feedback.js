const express = require("express");
const router = express.Router();

router.post("/", (req, res) => {
  // user feedback routes
});

router.post("/report", (req, res) => {
  // user feedback to crime
});

module.exports = router;
